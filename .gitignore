.claude/
.DS_Store

# Local sqlite test DB (CI/test runs use sqlite:///test.db)
backend/test.db
# compose-only interpolation values (see scripts/dev-mobile.sh)
.env

# local Playwright capture workspace (screenshots / videos)
.captures/
